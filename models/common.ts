
export interface ListResponse {
    success: string,
    data: any[],
    message: string,
    code: string
}

export interface BaseResponse {
    success: string,
    data: any,
    message: string,
    code: string
}